
import React, { Component } from 'react'
import {Button, Content} from '../components/buttons/styled'

export default class Signup extends Component {
    render() {
        return (
        <div>
            <Button bgColor={"red"}>Hola</Button>
            <Button bgColor={"blue"}>Adios</Button>
        </div>
        )
    }
}
